import React, { Component } from 'react';
import styled from 'styled-components';
import posed from 'react-pose';

const drawerPose = ({

})

const Drawer = styled(posed.div(drawerPose))`
    display: flex;
`

export default class state extends Component {
    render() {
        return (
            <div>

            </div>
        )
    }
}
